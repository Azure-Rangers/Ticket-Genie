trigger:
  branches:
    include:
      - main
  paths:
    include:
      - frontend/**
      - backend/**
      - Dockerfile
      - docker-compose.yml
      - nginx.conf
      - pyproject.toml
      - terraform/**
      - azure-pipelines.yml

variables:
  azureServiceConnection: "TicketGenie"
  resourceGroupName: "Azure_Rangers"
  acrName: "acrwebappprodticketgenie"
  acrLoginServer: "acrwebappprodticketgenie.azurecr.io"
  frontendImageRepository: "ticketgenie"
  backendImageRepository: "ticket-genie-backend"
  frontendWebAppName: "webapp-prod-frontend-ticketgenie"
  backendWebAppName: "webapp-prod-backend-ticketgenie"
  imageTag: "$(Build.BuildId)"

stages:
  - stage: BuildAndPush
    displayName: Build and Push Containers
    jobs:
      - job: Build
        displayName: Build container images
        pool:
          vmImage: ubuntu-latest
        steps:
          - checkout: self

          - task: AzureCLI@2
<<<<<<< HEAD
            displayName: Build, Archive, and Push FastAPI backend image to ACR
=======
            displayName: Build and Push FastAPI backend and Nginx frontend images to ACR
>>>>>>> d910f7bbeed9a89311a3cf7edbd0accb1f5a898d
            inputs:
              azureSubscription: $(azureServiceConnection)
              scriptType: bash
              scriptLocation: inlineScript
              inlineScript: |
                set -euo pipefail

<<<<<<< HEAD
                mkdir -p "$(Build.ArtifactStagingDirectory)/container-artifacts"

                # 1. Build backend image locally and export container archive
                docker build --target backend -t "$(backendImageRepository):$(imageTag)" -t "$(backendImageRepository):latest" -f Dockerfile .
                docker save "$(backendImageRepository):$(imageTag)" -o "$(Build.ArtifactStagingDirectory)/container-artifacts/backend-container.tar"

                # 2. Build and push to Azure Container Registry
                az acr build \
                  --registry "$(acrName)" \
=======
                # Log in to Azure Container Registry
                az acr login --name "$(acrName)"

                # Ensure buildx builder is initialized for registry caching
                docker buildx create --use --name pipeline-builder || docker buildx use pipeline-builder

                # 1. Build and Push Backend Image with remote registry buildcache
                docker buildx build \
                  --push \
>>>>>>> d910f7bbeed9a89311a3cf7edbd0accb1f5a898d
                  --target backend \
                  --cache-to type=registry,ref="$(acrLoginServer)/$(backendImageRepository):buildcache",mode=max \
                  --cache-from type=registry,ref="$(acrLoginServer)/$(backendImageRepository):buildcache" \
                  -t "$(acrLoginServer)/$(backendImageRepository):$(imageTag)" \
                  -t "$(acrLoginServer)/$(backendImageRepository):latest" \
                  -f Dockerfile .

<<<<<<< HEAD
          - task: AzureCLI@2
            displayName: Build, Archive, and Push Nginx frontend image to ACR
            inputs:
              azureSubscription: $(azureServiceConnection)
              scriptType: bash
              scriptLocation: inlineScript
              inlineScript: |
                set -euo pipefail

                # 1. Build frontend image locally and export container archive
                docker build --target frontend -t "$(frontendImageRepository):$(imageTag)" -t "$(frontendImageRepository):latest" -f Dockerfile .
                docker save "$(frontendImageRepository):$(imageTag)" -o "$(Build.ArtifactStagingDirectory)/container-artifacts/frontend-container.tar"

                # 2. Build and push to Azure Container Registry
                az acr build \
                  --registry "$(acrName)" \
=======
                # 2. Build and Push Frontend Image with remote registry buildcache
                docker buildx build \
                  --push \
>>>>>>> d910f7bbeed9a89311a3cf7edbd0accb1f5a898d
                  --target frontend \
                  --cache-to type=registry,ref="$(acrLoginServer)/$(frontendImageRepository):buildcache",mode=max \
                  --cache-from type=registry,ref="$(acrLoginServer)/$(frontendImageRepository):buildcache" \
                  -t "$(acrLoginServer)/$(frontendImageRepository):$(imageTag)" \
                  -t "$(acrLoginServer)/$(frontendImageRepository):latest" \
                  -f Dockerfile .

          - task: PublishPipelineArtifact@1
            displayName: Publish Container Image Archives to DevOps
            inputs:
              targetPath: '$(Build.ArtifactStagingDirectory)/container-artifacts'
              artifact: 'container-artifacts'
              publishLocation: 'pipeline'

  - stage: UpdateWebApp
    displayName: Update Web App Containers
    dependsOn: BuildAndPush
    condition: and(succeeded(), eq(variables['Build.SourceBranch'], 'refs/heads/main'))
    jobs:
      - deployment: DeployWebApp
        displayName: Point Web Apps at new container images
        environment: production
        pool:
          vmImage: ubuntu-latest
        strategy:
          runOnce:
            deploy:
              steps:
                - checkout: none

                - task: AzureCLI@2
                  displayName: Update backend and frontend web app containers and restart
                  inputs:
                    azureSubscription: $(azureServiceConnection)
                    scriptType: bash
                    scriptLocation: inlineScript
                    inlineScript: |
                      set -euo pipefail

                      # 1. Update Backend Web App (FastAPI REST API on Port 8000)
                      az webapp config container set \
                        --resource-group "$(resourceGroupName)" \
                        --name "$(backendWebAppName)" \
                        --docker-custom-image-name "$(acrLoginServer)/$(backendImageRepository):$(imageTag)" \
                        --docker-registry-server-url "https://$(acrLoginServer)"

                      az webapp config appsettings set \
                        --resource-group "$(resourceGroupName)" \
                        --name "$(backendWebAppName)" \
                        --settings WEBSITES_PORT=8000

                      az webapp restart \
                        --resource-group "$(resourceGroupName)" \
                        --name "$(backendWebAppName)"

                      # 2. Update Frontend Web App (Nginx UI on Port 80)
                      az webapp config container set \
                        --resource-group "$(resourceGroupName)" \
                        --name "$(frontendWebAppName)" \
                        --docker-custom-image-name "$(acrLoginServer)/$(frontendImageRepository):$(imageTag)" \
                        --docker-registry-server-url "https://$(acrLoginServer)"

                      az webapp config appsettings set \
                        --resource-group "$(resourceGroupName)" \
                        --name "$(frontendWebAppName)" \
                        --settings WEBSITES_PORT=80 BACKEND_API_URL="https://$(backendWebAppName).azurewebsites.net"

                      az webapp restart \
                        --resource-group "$(resourceGroupName)" \
                        --name "$(frontendWebAppName)"
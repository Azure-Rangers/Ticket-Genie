trigger:
  branches:
    include:
      - main
  paths:
    include:
      - app/**
      - backend/**
      - Dockerfile
      - pyproject.toml
      - terraform/**
      - azure-pipelines.yml

variables:
  azureServiceConnection: "TicketGenie"
  resourceGroupName: "Azure_Rangers"
  acrName: "acrwebappprodticketgenie"
  acrLoginServer: "acrwebappprodticketgenie.azurecr.io"
  frontendImageRepository: "ticketgenie"
  backendImageRepository: "ticket-genie-backend"
  frontendWebAppName: "webapp-prod-frontend-ticketgenie"
  backendWebAppName: "webapp-prod-backend-ticketgenie"
  imageTag: "$(Build.BuildId)"

stages:
  - stage: BuildAndPush
    displayName: Build and Push Containers
    jobs:
      - job: Build
        displayName: Build container images
        pool:
          vmImage: ubuntu-latest
        steps:
          - checkout: self

          - task: AzureCLI@2
            displayName: Build and push FastAPI backend image to ACR
            inputs:
              azureSubscription: $(azureServiceConnection)
              scriptType: bash
              scriptLocation: inlineScript
              inlineScript: |
                set -euo pipefail

                az acr build \
                  --registry "$(acrName)" \
                  --target backend \
                  --image "$(backendImageRepository):$(imageTag)" \
                  --image "$(backendImageRepository):latest" \
                  --file Dockerfile \
                  .

          - task: AzureCLI@2
            displayName: Build and push Streamlit frontend image to ACR
            inputs:
              azureSubscription: $(azureServiceConnection)
              scriptType: bash
              scriptLocation: inlineScript
              inlineScript: |
                set -euo pipefail

                az acr build \
                  --registry "$(acrName)" \
                  --target frontend \
                  --image "$(frontendImageRepository):$(imageTag)" \
                  --image "$(frontendImageRepository):latest" \
                  --file Dockerfile \
                  .

  - stage: UpdateWebApp
    displayName: Update Web App Containers
    dependsOn: BuildAndPush
    condition: and(succeeded(), eq(variables['Build.SourceBranch'], 'refs/heads/main'))
    jobs:
      - deployment: DeployWebApp
        displayName: Point Web Apps at new container images
        environment: production
        pool:
          vmImage: ubuntu-latest
        strategy:
          runOnce:
            deploy:
              steps:
                - checkout: none

                - task: AzureCLI@2
                  displayName: Update backend and frontend web app containers and restart
                  inputs:
                    azureSubscription: $(azureServiceConnection)
                    scriptType: bash
                    scriptLocation: inlineScript
                    inlineScript: |
                      set -euo pipefail

                      # 1. Update Backend Web App (FastAPI REST API on Port 8000)
                      az webapp config container set \
                        --resource-group "$(resourceGroupName)" \
                        --name "$(backendWebAppName)" \
                        --docker-custom-image-name "$(acrLoginServer)/$(backendImageRepository):$(imageTag)" \
                        --docker-registry-server-url "https://$(acrLoginServer)"

                      az webapp config appsettings set \
                        --resource-group "$(resourceGroupName)" \
                        --name "$(backendWebAppName)" \
                        --settings WEBSITES_PORT=8000

                      az webapp restart \
                        --resource-group "$(resourceGroupName)" \
                        --name "$(backendWebAppName)"

                      # 2. Update Frontend Web App (Streamlit UI on Port 8501)
                      az webapp config container set \
                        --resource-group "$(resourceGroupName)" \
                        --name "$(frontendWebAppName)" \
                        --docker-custom-image-name "$(acrLoginServer)/$(frontendImageRepository):$(imageTag)" \
                        --docker-registry-server-url "https://$(acrLoginServer)"

                      az webapp config appsettings set \
                        --resource-group "$(resourceGroupName)" \
                        --name "$(frontendWebAppName)" \
                        --settings WEBSITES_PORT=8501 BACKEND_API_URL="https://$(backendWebAppName).azurewebsites.net"

                      az webapp restart \
                        --resource-group "$(resourceGroupName)" \
                        --name "$(frontendWebAppName)"